# robots.txt for rpk.io
# https://www.robotstxt.org/

User-agent: *
Allow: /

# Private client review sessions. These pages also carry a noindex meta tag.
Disallow: /critiq/

# Tag filtering is client-side; the URLs are duplicates of /posts/.
Disallow: /*?tag=

# NOTE: /css/, /js/ and /fonts/ are deliberately crawlable. Blocking them stops
# Google rendering the page, which costs more than it saves.

Sitemap: https://rpk.io/sitemap.xml
